import 'package:flutter/material.dart';

class FavoriteScreen extends StatelessWidget {
  static const String name = 'favorite_screen';

  const FavoriteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}